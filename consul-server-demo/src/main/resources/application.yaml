spring:
  application:
    name: consul-provider
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: service-provider
provider:
  name: provider
server:
  port: 8505
